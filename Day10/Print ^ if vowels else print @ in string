vowels = "aeiouAEIOU"
se = "Booyaka"

transformed = ''
for char in se:
    if char in vowels:
        transformed += char + "^"
    else:
        transformed += char + "@"

print(transformed)

OUTPUT:
B@o^o^y@a^k@a^
