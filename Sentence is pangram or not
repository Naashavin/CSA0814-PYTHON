import string


sentence = input("Enter a sentence: ")


css = ''.join(e.lower() for e in sentence if e.isalpha())


as = set(string.ascii_lowercase)
sss = set(clean_sentence)


pangram = as.issubset(sss)


print(pangram)
